// src/app/(app)/onboarding/layout.tsx
// Layout limpo para a experiência de onboarding em ecrã inteiro
export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}